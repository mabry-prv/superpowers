# Knowledge Index

## Patterns

- **repo-conventions** — Base-branch fallback chain; `.gitignore` allow-listing; lint-artifact ignores — `patterns/repo-conventions.md`

## Gotchas

- **bash-scripts** — `producer | while array+=()` loses writes; session-start hook keys on `${PWD}` — `gotchas/bash-scripts.md`
- **test-fixtures** — Fixtures must violate the threshold they test; structural-test regexes bake in literal phrasing — `gotchas/test-fixtures.md`
